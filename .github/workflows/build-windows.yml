name: Build Windows

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # TODO: real build/packaging steps land in a later phase, once the
      # windows-control-surface app exists (see scripts/package-windows.ps1).
      - name: Placeholder build step
        run: echo "TODO: replace with real Windows .EXE packaging steps"
